# Comma-separated Hugging Face tokens. Keep real values in Replit Secrets.
HF_API_KEYS=
# Optional single-key fallback when HF_API_KEYS is not set.
HF_TOKEN=
# Optional image-to-image model override.
HF_IMAGE_EDIT_MODEL=timbrooks/instruct-pix2pix