<?xml version="1.0" encoding="utf-8"?>
<!--
  Local permissions plugin — merged by Capacitor into the app manifest during cap sync.
  Add any permission here to have it declared in the final APK.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Microphone — required for voice input in Studio and Study pages -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Camera — required for photo uploads and document scanning -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Storage (Android <= 12) -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!-- Granular media permissions (Android 13+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- Notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- In-app APK self-update — allows installing downloaded APK files -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Feature declarations for Play Store filtering -->
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

</manifest>
