# DaraQuiz Admin App

Admin-only mobile app for managing the DaraQuiz / xzily AI platform.

## Features
- Admin dashboard & platform stats
- Host management (approve / suspend hosts)
- Quiz management (create, edit, delete any quiz)
- App settings & update management
- About / content pages

## Build the APK

```bash
npm install
npx cap sync android
cd android && ./gradlew assembleRelease
```

Output: `android/app/build/outputs/apk/release/app-release.apk`

## App ID
`com.darapet.adminquiz` — separate from the main app (`com.darapet.smart`)

## Folder structure
```
daraquiz-admin/
├── capacitor.config.json
├── package.json
├── www/
│   ├── index.html          ← splash → admin-dashboard.html
│   ├── admin.html
│   ├── admin-dashboard.html
│   ├── admin-hosts.html
│   ├── admin-settings.html
│   ├── admin-update.html
│   ├── login.html
│   ├── js/
│   └── css/
└── android/                ← generated by: npx cap sync
```
